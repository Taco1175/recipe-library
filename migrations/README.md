# Migrations (Historical)

These SQL files were used during the original Supabase deployment and are kept for reference only. The app now runs on PocketBase (self-hosted on Raspberry Pi). PocketBase manages its own schema via the admin UI.
